# Templates

This open-source package keeps a single active storefront template:

- `modave`

The Modave template is selected by the package scripts and does not require passing a template id manually.
